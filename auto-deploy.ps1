# DISABLED: the old 5-second auto-push watcher has been retired.
# It was committing every keystroke/line-ending change straight to production.
# To deploy intentionally when your work is ready, run:
#     powershell -ExecutionPolicy Bypass -File .\deploy.ps1
Write-Host "auto-deploy watcher is disabled. Use deploy.ps1 to deploy when ready." -ForegroundColor Yellow
